# uri-bushey.com

Archive of the hand-written apex site, currently served over SFTP. Stored here
so it has a home in git; **not deployed** — Hugo ignores this folder.

To retire the SFTP workflow later, move `index.html` into `static/` and point
the Pages custom domain at the apex instead of `blog.`.
